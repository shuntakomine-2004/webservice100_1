17.HTTPメソッドの違い
- GET: サーバーからデータを取得する。URLのクエリパラメータを使って指定された情報を読み取る。
- POST: サーバーに新しいデータを送信して作成する。フォーム送信やJSON送信で使われ、サーバー側の状態を変える。
- PUT: 既存のデータをすべて上書きして更新する。指定したリソースを完全に置き換えるため、全フィールドを送る必要がある。
- PATCH: 既存のデータの一部を更新する。PUTが全体更新なのに対し、PATCHは部分的な変更だけを行う。
- DELETE: 指定したリソースをサーバーから削除する。実行後はデータが存在しなくなる。